const bill=275;
console.log(50<=bill && bill<=300 ? `The bill was ${bill}, the tip was 
${bill * 0.15}, and the total value ${bill + bill * 0.15}` : 
`The bill was ${bill}, the tip was 
${bill * 0.20}, and the total value ${bill + bill * 0.20}`);